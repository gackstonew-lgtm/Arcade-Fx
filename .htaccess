# Arcade FX — Root Redirect & Directory Index Configuration
DirectoryIndex landing.html index.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirect root request to landing.html
  RewriteRule ^$ landing.html [L]
</IfModule>
